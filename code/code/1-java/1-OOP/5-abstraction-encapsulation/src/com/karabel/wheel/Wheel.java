package com.karabel.wheel;

// interface / api / spec / object's design coding
public interface Wheel {
    public abstract int rotate(int speed);
}

//imp-note : we can't create object for an interface
